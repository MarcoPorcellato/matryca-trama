# Python read-contract qualification evidence

## Result

Qualified only for synthetic OG fixtures and `graph.identify`, `page.read`,
and complete ordered `block.subtree.read.complete`. User graphs, DB, writes,
events, Shadow, synchronization, export, recovery, UI/Nodi, distribution,
performance, and network remain unsupported.

## Exact evidence

- Commit: `862c5c89157f28c1985cde6145fc2c8af04a70b4`; tree: `f1dacc9b30c993b2b69a48c20e73281732e781b3`
- Ubuntu 24.04.4 LTS, x86_64; runner `ubuntu-24.04` `20260823.283.1`
- Python `3.12.3`; uv `0.12.9` (`x86_64-unknown-linux-gnu`)
- Lock SHA-256: `1987e031fa247cfa67dd0f991385e6bcbb15aab3f1c7f097a2af379861e24bfb`
- Fixture manifest SHA-256: `2b25bad6c44f7c11e09a1bb802437dd8cd49195691d78884b96b4ab589763818`

- Python contracts run: [33631902659](https://github.com/MarcoPorcellato/matryca-trama/actions/runs/33631902659)
- Dependency Review: [33631902709](https://github.com/MarcoPorcellato/matryca-trama/actions/runs/33631902709), `SUCCESS`
- Foundation CI: [33631902680](https://github.com/MarcoPorcellato/matryca-trama/actions/runs/33631902680), `SUCCESS`

## Executed commands and outcomes

- `python --version`: `SUCCESS`
- `uv --version`: `SUCCESS`
- `uv sync --locked --all-packages`: `SUCCESS`
- `uv run --all-packages python -m unittest discover -s tests/contracts -v`:
  `SUCCESS` (38 tests, including static read-only and determinism checks)
- `uv run --all-packages python -m unittest discover -s tests/containment -v`:
  `SUCCESS` (6 tests)
- `uv run --all-packages python -m unittest tests.integration.test_plumber_consumer -v`:
  `SUCCESS` (7 tests)
- `uv run --all-packages python -m unittest tests.test_foundation_validator -v`:
  `SUCCESS` (8 tests)

## Disposition

All capabilities outside this synthetic OG slice require a separate reviewed
contract and evidence gate.
