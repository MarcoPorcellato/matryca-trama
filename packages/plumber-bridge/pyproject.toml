[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "trama-plumber-bridge"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["trama-contracts"]

[tool.hatch.build.targets.wheel]
packages = ["src/trama_plumber_bridge"]
